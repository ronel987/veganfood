/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['dsnyk1xc1sxw4.cloudfront.net'],        
    },
};

module.exports = nextConfig;
